[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
cmds = ["cd kalshi-dashboard && npm ci"]

[phases.build]
cmds = ["cd kalshi-dashboard && npm run build"]

[start]
cmd = "cd kalshi-dashboard && npm run preview -- --host 0.0.0.0 --port $PORT"
